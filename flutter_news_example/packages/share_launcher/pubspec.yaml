name: share_launcher
description: A launcher that handles share functionality on a device.
version: 1.0.0+1
publish_to: none

environment:
  sdk: ^3.11.0

dependencies:
  equatable: ^2.0.8
  flutter:
    sdk: flutter
  share_plus: ^12.0.1
  share_plus_platform_interface: ^6.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  mocktail: ^1.0.4
  plugin_platform_interface: ^2.1.8
  url_launcher_platform_interface: ^2.3.2
  very_good_analysis: ^10.2.0
